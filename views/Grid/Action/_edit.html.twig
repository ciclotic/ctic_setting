{% import '/Macro/buttons.html.twig' as buttons %}

{% set metadata = grid.data.requestConfiguration.metadata %}

{{ buttons.edit(path('GET_' ~ metadata.parameters.route.root ~ '_' ~  metadata.parameters.route.actions.update, {'id': data.id}), action.label, data.id, true) }}
