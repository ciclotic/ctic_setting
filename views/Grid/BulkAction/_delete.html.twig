{% import '/Macro/buttons.html.twig' as buttons %}

{% set metadata = grid.data.requestConfiguration.metadata %}

{{ buttons.bulkDelete(path('POST_' ~ metadata.parameters.route.root ~ '_' ~  metadata.parameters.route.actions.bulkDelete), action.label, true) }}
