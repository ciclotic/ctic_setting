<div class="row">
    <div class="col-md-12">
        <div class="row">
            <div class="col-md-4">
                <label>Nombre</label>
                {{ resource.name }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-2">
                <label>Habilitado</label>
                {{ (resource.enabled is empty)? 'No' : 'Si' }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <label>Empresa por defecto</label>
                {{ resource.defaultCompany.businessName }}
            </div>
        </div>
        <div class="row">
            <div class="col-md-4">
                <label>Cuenta</label>
                {{ resource.account.name }}
            </div>
        </div>
        <br>
        <div class="cancel-button">
            {% include '/Form/Buttons/_cancel.html.twig' with {'paths': {'cancel': path('GET_' ~ metadata.parameters.route.root ~ '_' ~  metadata.parameters.route.actions.list, configuration.vars.route.parameters|default({}))}} %}
        </div>
    </div>
</div>
